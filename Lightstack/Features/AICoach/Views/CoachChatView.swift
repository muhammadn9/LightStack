import SwiftUI

/// Conversational chat view with the AI coach.
/// Ephemeral per session — messages are held in memory only, not persisted.
/// Shared across Today and Month Plan tabs.
struct CoachChatView: View {
    // TODO: Phase 2 — Implement coach chat UI

    var body: some View {
        Text("Coach Chat — TODO")
    }
}
