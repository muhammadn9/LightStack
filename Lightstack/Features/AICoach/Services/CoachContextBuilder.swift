import Foundation

/// Builds the AI prompt context from saved data:
/// rolling summary, last 1-2 raw sessions, profile, PRs, and today's input.
/// Target prompt size: 700-900 tokens per call.
final class CoachContextBuilder {
    // TODO: Phase 2 — Implement context assembly from profile + history + summaries
}
