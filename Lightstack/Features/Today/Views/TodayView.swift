import SwiftUI

/// Entry point for the Today tab.
/// Shows the current day's workout plan or a quick-start setup screen.
struct TodayView: View {
    // TODO: Phase 1 — Show plan or WorkoutSetupView

    var body: some View {
        NavigationStack {
            Text("Today — Placeholder")
                .navigationTitle("Today")
        }
    }
}
