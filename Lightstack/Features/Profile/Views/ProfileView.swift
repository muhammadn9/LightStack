import SwiftUI

/// Profile tab: stats (streak, total sessions, PRs), equipment picker,
/// split day editor, coach preferences, and account settings.
struct ProfileView: View {
    // TODO: Phase 3 — Implement profile screen

    var body: some View {
        NavigationStack {
            Text("Profile — Placeholder")
                .navigationTitle("Profile")
        }
    }
}
